ingress:
  enabled: true
  hostname: {{ awx_hostname }}
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /
  path: /
  tls: []
service:
  type: ClusterIP